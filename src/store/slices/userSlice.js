import { createSlice } from '@reduxjs/toolkit';

// this slice is for the user information

const date = new Date();
weekday
const initialState = {
  isEssSwitch: true,
  isExpanded: false,
  isAdministrator: true,
  dateAndWether : {
    date:
  }
};


